*** Probes ***

+ FPing
binary = /usr/bin/fping

+ Curl
binary = /usr/bin/curl
step = 60
# a default for this target-specific variable
urlformat = http://%host%/

+EchoPingHttp
binary = /usr/bin/echoping
forks = 5
offset = 50%
step = 300
